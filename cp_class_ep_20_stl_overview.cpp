#include<bits/stdc++.h>

using namespace std;

#define pb push_back
typedef long long int ll;
typedef unsigned long long ull;
const int N=1e5+10;


// containers[ sequential--> {vector,stack,queue, pair(class)}, ordered{maps,set,multi set,multimap},unordered{unorderd map,unordered set}]


// nested containers.

//vector<vector<int>;
//map<int,vector<int>>;
//set<pair<int,string>>;
//vector< <int,set<int>>

// iterators
//-- point to memory address of containers.
//---- begin(),end()
// vector<int>:: iterator it; how to write it in short;
//continaity for containers. continutity and discontinuetity

//Algorithms

//upper bound, lower bound,sort(comparator),max-element,min-elements,accumulate, reverse,count ,find ,next-permatations, prev-permatations;

// functors

// classes which can act as functions; but it is a object. at first we create a class with default constructor and then void operator ()(int val){ num+=val;}

int main(){

}
