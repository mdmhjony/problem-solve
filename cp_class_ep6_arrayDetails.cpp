#include<bits/stdc++.h>
using namespace std;

const int n=1e7;// const variable in cp max golbal is 10^7.
const int n1=2e7;// const variable 2*10^7

// global array save in DATA Segment has no memory limit

 int main(){
   // we can use all type data type with bool,int,float,double, but cann't use char
   // use alternative string datatype.
   // rows and coloumn
   //int a[4][3];    // a 4 collection then we use [3] so it means a[4] 3 collection
   //int a[4][3][2];    // 3d a[4][3] ar 2 collection
//   int n=1e7;// segmentation fault array because of size but locally we can use 1e5 big size but global would be const variable
//   int a[n]; //  use in local array in cp limit is 10^5.
 //local array is store in stack array size.8 MB. has a chances overflow size.
//   a[n-1]=7;
//   cout<<a[n-1];






 }
